# WrenchGeeks

Local-first vehicle service job queue baseline.

```bash
npm test
npm start
```

Endpoints: `GET /health`, `GET /api/jobs`, and `POST /api/jobs` with `title` and `vehicle`. Add technician assignment, parts inventory, notifications, and durable persistence behind the current job contract.
